{
  "name": "intellectro",
  "private": true,
  "version": "0.0.1-alpha",
  "type": "module",
  "workspaces": [
    "packages/*",
    "agents/*"
  ],
  "engines": {"node": ">=20"},
  "scripts": {
    "test": "node --test",
    "check": "find packages agents -name '*.js' -type f -print0 2>/dev/null | xargs -0 -r -n1 node --check"
  }
}
